services:
  - type: web
    name: eval-ai
    runtime: node
    buildCommand: npm install
    startCommand: node backend/server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
